VITE_API_URL=http://localhost:8000

# Must match backend/.env's REVERB_APP_KEY/HOST/PORT/SCHEME (Decision 25) —
# only the public key, never REVERB_APP_SECRET, which stays server-side only.
VITE_REVERB_APP_KEY=9bl53dtynr9gflf8kjbu
VITE_REVERB_HOST=localhost
VITE_REVERB_PORT=8080
VITE_REVERB_SCHEME=http
